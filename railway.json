{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --prefix external-render",
    "watchPatterns": [
      "external-render/**"
    ]
  },
  "deploy": {
    "startCommand": "cd external-render && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

